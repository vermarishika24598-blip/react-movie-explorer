# netlify.toml

[build]
  # Command to build your frontend
  command = "npm run build"
  # Folder where the build output goes
  publish = "dist"

# Redirect all routes to index.html (for SPA routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
